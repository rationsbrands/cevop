{
  "name": "cevop",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-tenant restaurant ordering platform",
  "scripts": {
    "dev:server": "npm run dev -w cevop-server",
    "dev:customer": "npm run dev -w cevop-customer-pwa",
    "dev:service": "npm run dev -w cevop-service-display",
    "dev:admin": "npm run dev -w cevop-admin-dashboard",
    "dev:ops": "npm run dev -w cevop-ops-dashboard",
    "build:all": "npm run build --workspaces --if-present",
    "db:migrate": "npm run db:migrate -w cevop-server",
    "db:seed": "npm run db:seed -w cevop-server",
    "db:generate": "npm run db:generate -w cevop-server",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "prettier --write"
    ],
    "*.{json,css,md}": [
      "prettier --write"
    ]
  },
  "workspaces": [
    "apps/*",
    "server",
    "shared"
  ],
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.59.4",
    "@typescript-eslint/parser": "^8.59.4",
    "eslint": "^10.4.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  }
}
